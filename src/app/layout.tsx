import type { Metadata } from "next";
import { IBM_Plex_Mono, Manrope } from "next/font/google";
import "./globals.css";

const manrope = Manrope({
  variable: "--font-manrope",
  subsets: ["latin"],
});

const plexMono = IBM_Plex_Mono({
  variable: "--font-plex-mono",
  subsets: ["latin"],
  weight: ["400", "500"],
});

export const metadata: Metadata = {
  title: "Rental Lender Calculator",
  description:
    "Configure lender-specific rental property criteria and calculate live lending outcomes for mortgage clients.",
};

const bodyClassName = `${manrope.variable} ${plexMono.variable} min-h-full`;

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="h-full">
      <body className={bodyClassName}>{children}</body>
    </html>
  );
}
